import { ValueControllerDirective } from './value-controller.directive';

describe('ValueControllerDirective', () => {
  it('should create an instance', () => {
    const directive = new ValueControllerDirective();
    expect(directive).toBeTruthy();
  });
});
